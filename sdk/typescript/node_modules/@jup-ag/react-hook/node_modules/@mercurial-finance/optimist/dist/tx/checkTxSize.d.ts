import { Transaction, VersionedTransaction } from '@solana/web3.js';
import { TransactionError } from './errors';
export declare const checkTxSize: (transaction: VersionedTransaction | Transaction) => {
    error: TransactionError;
} | undefined;
