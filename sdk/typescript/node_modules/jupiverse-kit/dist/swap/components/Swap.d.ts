import React from "react";
interface SwapProps {
    rpcUrl: string;
    referralKey?: string;
    platformFeeBps?: number;
    apiKey?: string;
}
declare const Swap: ({ rpcUrl, referralKey, platformFeeBps, apiKey }: SwapProps) => React.JSX.Element;
export default Swap;
