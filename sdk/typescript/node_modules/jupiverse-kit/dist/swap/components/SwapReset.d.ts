import React from "react";
interface SwapResetProps {
    onRefresh: () => void;
    isCalculating: boolean;
    isSwapping: boolean;
}
declare const SwapReset: ({ onRefresh, isCalculating, isSwapping, }: SwapResetProps) => React.JSX.Element;
export default SwapReset;
