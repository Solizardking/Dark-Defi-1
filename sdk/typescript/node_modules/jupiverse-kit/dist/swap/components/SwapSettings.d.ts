import React from "react";
interface SwapSettingsProps {
    slippage: number;
    onSlippageChange: (slippage: number) => void;
    onSave: () => void;
}
declare const SwapSettings: ({ slippage, onSlippageChange, onSave, }: SwapSettingsProps) => React.JSX.Element;
export default SwapSettings;
