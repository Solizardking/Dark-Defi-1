import React from "react";
import { Token } from "../utils/interfaces";
declare const SwapTokenButton: ({ token, onClick, }: {
    token: Token | null;
    onClick: () => void;
}) => React.JSX.Element;
export default SwapTokenButton;
