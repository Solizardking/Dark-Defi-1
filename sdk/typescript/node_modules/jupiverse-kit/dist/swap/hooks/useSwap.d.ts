import { Token, QuoteResponse } from "../utils/interfaces";
interface UseSwapProps {
    rpcUrl: string;
    referralKey?: string;
    platformFeeBps?: number;
    apiKey?: string;
}
export declare const useSwap: ({ rpcUrl, referralKey, platformFeeBps, apiKey, }: UseSwapProps) => {
    getQuote: (inputToken: Token, outputToken: Token, amount: number, slippageBps?: number) => Promise<QuoteResponse>;
    executeSwap: (quoteResponse: QuoteResponse) => Promise<string | null>;
    calculateOutputAmount: (inputToken: Token, outputToken: Token, inputAmount: number) => Promise<string>;
    calculateMaxInput: (inputToken: Token, balance: number) => Promise<number>;
    loading: boolean;
    error: string | null;
};
export {};
