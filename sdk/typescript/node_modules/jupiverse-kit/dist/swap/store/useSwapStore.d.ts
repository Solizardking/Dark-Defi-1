import { SwapState, SwapActions } from "../utils/interfaces";
export declare const useSwapStore: import("zustand").UseBoundStore<Omit<import("zustand").StoreApi<SwapState & SwapActions>, "persist"> & {
    persist: {
        setOptions: (options: Partial<import("zustand/middleware").PersistOptions<SwapState & SwapActions, {
            tokenFrom: import("../utils/interfaces").Token | null;
            tokenTo: import("../utils/interfaces").Token | null;
            amountFrom: string;
            amountTo: string;
            slippage: number;
        }>>) => void;
        clearStorage: () => void;
        rehydrate: () => Promise<void> | void;
        hasHydrated: () => boolean;
        onHydrate: (fn: (state: SwapState & SwapActions) => void) => () => void;
        onFinishHydration: (fn: (state: SwapState & SwapActions) => void) => () => void;
        getOptions: () => Partial<import("zustand/middleware").PersistOptions<SwapState & SwapActions, {
            tokenFrom: import("../utils/interfaces").Token | null;
            tokenTo: import("../utils/interfaces").Token | null;
            amountFrom: string;
            amountTo: string;
            slippage: number;
        }>>;
    };
}>;
export declare const useSwapSelector: <T>(selector: (state: SwapState) => T) => T;
