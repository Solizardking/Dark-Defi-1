import JSBI from "jsbi";
export interface UltraQuoteResponse {
    inputMint: string;
    inAmount: string;
    outputMint: string;
    outAmount: string;
    otherAmountThreshold: string;
    priceImpactPct: string;
    routePlan: {
        swapInfo: {
            inputMint: string;
            inAmount: string;
            outputMint: string;
            outAmount: string;
            ammKey: string;
            label: string;
            feeAmount: JSBI;
            feeMint: string;
        };
        percent: number;
    }[];
    contextSlot: number;
    transaction: string | null;
    swapType: "ultra";
    gasless: boolean;
    requestId: string;
    prioritizationFeeLamports?: number;
    feeBps: number;
}
export interface UltraSwapQuoteParams {
    inputMint: string;
    outputMint: string;
    amount: string;
    taker?: string;
}
export interface UltraSwapResponse {
    signature: string;
    status: string;
}
interface UltraSwapService {
    getQuote(params: UltraSwapQuoteParams): Promise<UltraQuoteResponse>;
    submitSwap(signedTransaction: string, requestId: string): Promise<UltraSwapResponse>;
}
declare class UltraSwapService implements UltraSwapService {
    private BASE_URL;
    private ROUTE;
}
export declare const ultraSwapService: UltraSwapService;
export {};
