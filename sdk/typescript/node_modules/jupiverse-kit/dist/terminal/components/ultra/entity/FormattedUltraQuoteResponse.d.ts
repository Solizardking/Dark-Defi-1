import { PublicKey } from "@solana/web3.js";
import JSBI from "jsbi";
import { Infer } from "superstruct";
export declare const FormattedUltraQuoteResponse: import("superstruct").Struct<{
    inputMint: PublicKey;
    outputMint: PublicKey;
    slippageBps: number;
    inAmount: JSBI;
    outAmount: JSBI;
    otherAmountThreshold: JSBI;
    priceImpactPct: string;
    routePlan: {
        swapInfo: {
            label: string;
            inputMint: string;
            outputMint: string;
            ammKey: PublicKey;
            inAmount: JSBI;
            outAmount: JSBI;
            feeAmount: JSBI;
            feeMint: PublicKey;
        };
        percent: number;
    }[];
    contextSlot: number;
    transaction: string | null;
    swapType: "aggregator" | "rfq";
    gasless: boolean;
    requestId: string;
    feeBps: number;
    computedAutoSlippage?: number | undefined;
    prioritizationFeeLamports?: number | undefined;
}, {
    inputMint: import("superstruct").Struct<PublicKey, null>;
    inAmount: import("superstruct").Struct<JSBI, null>;
    outputMint: import("superstruct").Struct<PublicKey, null>;
    outAmount: import("superstruct").Struct<JSBI, null>;
    otherAmountThreshold: import("superstruct").Struct<JSBI, null>;
    priceImpactPct: import("superstruct").Struct<string, null>;
    routePlan: import("superstruct").Struct<{
        swapInfo: {
            label: string;
            inputMint: string;
            outputMint: string;
            ammKey: PublicKey;
            inAmount: JSBI;
            outAmount: JSBI;
            feeAmount: JSBI;
            feeMint: PublicKey;
        };
        percent: number;
    }[], import("superstruct").Struct<{
        swapInfo: {
            label: string;
            inputMint: string;
            outputMint: string;
            ammKey: PublicKey;
            inAmount: JSBI;
            outAmount: JSBI;
            feeAmount: JSBI;
            feeMint: PublicKey;
        };
        percent: number;
    }, {
        swapInfo: import("superstruct").Struct<{
            label: string;
            inputMint: string;
            outputMint: string;
            ammKey: PublicKey;
            inAmount: JSBI;
            outAmount: JSBI;
            feeAmount: JSBI;
            feeMint: PublicKey;
        }, {
            ammKey: import("superstruct").Struct<PublicKey, null>;
            label: import("superstruct").Struct<string, null>;
            inputMint: import("superstruct").Struct<string, null>;
            outputMint: import("superstruct").Struct<string, null>;
            inAmount: import("superstruct").Struct<JSBI, null>;
            outAmount: import("superstruct").Struct<JSBI, null>;
            feeAmount: import("superstruct").Struct<JSBI, null>;
            feeMint: import("superstruct").Struct<PublicKey, null>;
        }>;
        percent: import("superstruct").Struct<number, null>;
    }>>;
    slippageBps: import("superstruct").Struct<number, null>;
    contextSlot: import("superstruct").Struct<number, null>;
    computedAutoSlippage: import("superstruct").Struct<number | undefined, null>;
    transaction: import("superstruct").Struct<string | null, null>;
    swapType: import("superstruct").Struct<"aggregator" | "rfq", {
        aggregator: "aggregator";
        rfq: "rfq";
    }>;
    gasless: import("superstruct").Struct<boolean, null>;
    requestId: import("superstruct").Struct<string, null>;
    prioritizationFeeLamports: import("superstruct").Struct<number | undefined, null>;
    feeBps: import("superstruct").Struct<number, null>;
}>;
export type FormattedUltraQuoteResponse = Infer<typeof FormattedUltraQuoteResponse>;
