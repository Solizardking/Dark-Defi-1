import React from "react";
import { DisplayMode } from "../utils/types";
export declare const getTerminalPlaceholder: (displayMode: DisplayMode, integratedTargetId?: string | null) => React.JSX.Element | null;
